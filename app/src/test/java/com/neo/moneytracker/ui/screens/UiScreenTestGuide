| Step | Code                                                                                                       | What It Tests                                               | Why It's Important                            |
| ---- | ---------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- | --------------------------------------------- |
| 1️⃣  | `composeRule.onNodeWithContentDescription("Add").performClick()`                                           | Simulates tapping the "Add" button                          | Starts the add transaction flow               |
| 2️⃣  | `composeRule.onNodeWithText("expenses").assertIsDisplayed()`                                               | Verifies that the **category option** "expenses" is visible | Ensures UI shows expected categories          |
| 3️⃣  | `composeRule.onNodeWithText("expenses").performClick()`                                                    | Selects the **"expenses"** category                         | Tests category selection logic                |
| 4️⃣  | `composeRule.onNodeWithContentDescription("Groceries").assertIsDisplayed()`                                | Verifies "Groceries" subcategory is shown                   | Confirms subcategories are correctly loaded   |
| 5️⃣  | `composeRule.onNodeWithContentDescription("Groceries").performClick()`                                     | Selects the "Groceries" subcategory                         | Ensures subcategory click works               |
| 6️⃣  | `composeRule.onNodeWithTag("subcategoryGroceries").assertIsSelected()`                                     | Asserts the **"Groceries"** subcategory is selected         | Confirms selection state is correctly updated |
| 7️⃣  | `composeRule.onNodeWithTag("AmountField").assertExists().performTextInput("500")`                          | Types `500` in the amount field                             | Tests input functionality of amount           |
| 8️⃣  | `composeRule.onNodeWithText("500").assertIsDisplayed()`                                                    | Verifies that `500` appears on screen                       | Confirms input is displayed properly          |
| 9️⃣  | `composeRule.onNodeWithText("500").assertTextEquals("500")`                                                | Ensures the value is exactly `500`                          | Validates correctness of input                |
| 🔟   | `composeRule.onNodeWithTag("NoteField").performTextInput("Monthly groceries")`                             | Types `Monthly groceries` as the note                       | Tests note field input                        |
| 🔟+1 | `composeRule.onNodeWithText("Monthly groceries").assertIsDisplayed()`                                      | Ensures the note is displayed                               | Confirms input handling is proper             |
| 🔟+2 | `composeRule.onNodeWithContentDescription("Check Icon").assertExists().assertIsDisplayed().performClick()` | Clicks the "Check Icon" to save                             | Tests if save action works via icon click     |
